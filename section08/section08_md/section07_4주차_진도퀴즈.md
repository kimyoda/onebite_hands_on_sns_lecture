## section07_4주차 quiz

### 1. 인증 과정에서 Zustand를 활용한 세션 관리의 주된 목적은?

A. 데이터베이스 영구 데이터를 저장하기 위함
**B. 사용자 인증을 위한 전역 상태를 컴포넌트 간에 관리하기 위함** -> Zustand는 전역 상태 관리 라이브러리, 사용자 인증 상태처럼 여러 컴포넌트에서 공유해야 하는 데이터를 효율적으로 관리하도록 돕는다. 세션 정보도 해당된다.
C. 서버 측 요청 로깅을 처리하기 위함
D. 사용자 자격 증명을 전송 전 암호화하기 위함.

### 2. 웹 애플리케이션 라우트 가드(Route Guard)의 주요 기능은?

A. 데이터베이스 쿼리 최적화를 통해 로딩 속도를 높인다
**B. 특정 라우트에 대한 무단 접근을 방치하고 보호한다** -> 라우트 가드는 특정 페이지나 리소스에 접근하기 전에 사용자 인증 상태나 권한을 확인, 인가되지 않은 접근을 막는 보안 기능이다. 경로 보호에 필수적인 요소이다.
C. 페이지 콘텐츠를 주기적으로 자동으로 새로 고침
D. 네트워크 트래픽 압축으로 성능을 향상시킨다.

### 3. 클라이언트 측에서 이미지 미리보기를 표시할 때 메모리 누수를 방지하는 방법은?

A. 더 큰 이미지 파일을 사용하여 재그리기를 줄인다
**B. 이미지 또는 객체 URL을 사용 후 자동으로 해체한다** -> 이미지 미리보기 생성된 객체 URL은 사용 후 반드시 해체해야 메모리 누수를 막을 수 있다. `URL_reovkeObjectURL()` 같은 함수로 명시적으로 해제하는 것이 중요하다.
C. 모든 이미지 데이터를 로컬 저장소에 직접 저장한다
D. 모든 클라이언트 측 이미지 처리를 비활성화한다.

### 4. 콘텐츠 목록 표시에 무한 스크롤을 구현하는 주된 이점은 무엇인가?

A. 서버에서 로드되는 총 항목수를 줄인다
**B. 초기 페이지 로드 속도와 사용자 경험을 향시킨다** -> 무한 스크롤은 초기 페이지 로드 시 적은 양의 데이터만 빠르게 표시, 사용자가 스크롤하면 추가 데이터를 로드하여 사용자 경험을 향상시키는데 기여한다.
C. 목록에 있는 모든 콘텐츠에 오프라인 접근을 가능하게 한다
D. 모든 콘텐츠를 한 번에 스크롤 없이 볼 수 있도록 한다.

### 5. 사용자 생성 게시물에 대한 수정/삭제 버튼의 조건부 렌더링이 중요한 이유는 무엇인가?

A. 불필요한 버튼 표시로 인한 서버 부하를 줄이기 위함
B. 다양한 게시물 유형 전반에 걸쳐 시각적 일관성을 높이기 위함
**C. 무단 수정을 방지하고 사용자 경험을 개선하기 위함** -> 조건부 렌더링을 통해 게시물 작성자에게만 수정/삭제 버튼을 보여줘, 데이터 무결성을 보호하고 사용자에게 필요한 기능만 제공하여 혼란을 줄일 수 있다.
D. 관리자가 모든 게시물을 즉시 편집할 수 있도록 허용하기 위함
