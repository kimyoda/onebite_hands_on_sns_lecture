## 2.5 Shadcn/ui_2

### Shadcn/ui

### 05. 컴포넌트 예시

- button의 varaint 종류

#### 🔘 버튼 Variant (변형)

| Variant       | 설명                         |
| :------------ | :--------------------------- |
| `default`     | 기본 버튼 (주요 액션)        |
| `outline`     | 테두리만 있는 버튼           |
| `ghost`       | 배경 없는 버튼               |
| `secondary`   | 보조 버튼                    |
| `destructive` | 삭제/위험 액션 버튼 (빨간색) |
| `link`        | 링크처럼 보이는 버튼         |

---

#### 📏 size 종류

| size      | 높이        | 주요 용도                   |
| :-------- | :---------- | :-------------------------- |
| `default` | h-9 (36px)  | 일반적인 버튼               |
| `sm`      | h-8 (32px)  | 작은 버튼                   |
| `lg`      | h-10 (40px) | 큰 버튼                     |
| `icon`    | 36×36px     | 아이콘만 있는 정사각형 버튼 |
| `xs`      | h-6 (24px)  | 아주 작은 버튼              |

---

#### Input 텍스트 입력 필드

```bash
npx shadcn@latest add input
```

- 기본 HTML `<input>` 에 shadcn/ui 스타일을 적용한 컴포넌트다.

```tsx
import { Input } from "@/components/ui/input";

{
  /* 실습 코드 */
}
<Input placeholder="입력..." />;
```

- 주요 사용 패턴

```tsx
{
  /* 기본 */
}
<Input type="text" placeholder="텍스트 입력" />;

{
  /* 이메일 */
}
<Input type="email" placeholder="이메일 입력" />;

{
  /* 비밀번호 */
}
<Input type="password" placeholder="비밀번호" />;

{
  /* 파일 업로드 */
}
<Input type="file" />;

{
  /* 비활성화 */
}
<Input disabled placeholder="입력 불가" />;

{
  /* Label과 함께 사용 (접근성) */
}
<div className="flex flex-col gap-1.5">
  <Label htmlFor="email">이메일</Label>
  <Input id="email" type="email" placeholder="example@email.com" />
</div>;

{
  /* 버튼과 함께 사용 */
}
<div className="flex gap-2">
  <Input placeholder="검색어 입력" />
  <Button>검색</Button>
</div>;
```

> Input 컴포넌트는 기본 HTML Input 요소를 그대로 확장한다. `type`, `placeholder`, `disabled` 등 모든 HTML input 속성을 그대로 사용할 수 있다.

---

### Textarea - 여러 줄 텍스트 입력

```bash
npx shadcn@latest add textarea
```

- 여러 줄 텍스트 입력을 위한 컴포넌트이다.

```tsx
import { Textarea } from "@/components/ui/textarea";

{
  /* 실습 코드 */
}
<Textarea />;
```

- 주요 사용 패턴

```tsx
{
  /* 기본 */
}
<Textarea placeholder="내용을 입력하세요." />;

{
  /* 비활성화 */
}
<Textarea disabled placeholder="입력 불가" />;

{
  /* Label과 함께 사용 */
}
<div className="flex flex-col gap-1.5">
  <Label htmlFor="message">메시지</Label>
  <Textarea id="message" placeholder="메시지를 입력하세요." />
</div>;

{
  /* 버튼과 함께 사용 (채팅 입력창 패턴) */
}
<div className="flex flex-col gap-2">
  <Textarea placeholder="내용 입력..." />
  <Button className="self-end">전송</Button>
</div>;
```

#### 📝 Input vs Textarea 비교

| 항목           | Input                  | Textarea         |
| :------------- | :--------------------- | :--------------- |
| **HTML 태그**  | `<input>`              | `<textarea>`     |
| **입력 줄 수** | 한 줄                  | 여러 줄          |
| **높이**       | 고정 (h-9)             | 가변             |
| **주요 용도**  | 이메일, 비밀번호, 검색 | 본문, 댓글, 메모 |
| **type prop**  | 있음                   | 없음             |

> 공식 문서: Textarea 컴포넌트는 기본 HTML `<textarea>` 요소를 확장한다. `rows`, `cols`, `maxLength`등 모든 HTML textarea 속성을 사용할 수 있다.

---

### Sonner - Toast 알림 (토스트 메시지)

```bash
npx shadcn@latest add sonner
```

- Sonner는 **화면 한쪽 구석에 잠깐 나타났다 사라지는 알림(Toast)을** 위항 컴포넌트이다.
- shadcn/ui는 자체 Toast대신 `sonner`라이브러리를 제공한다.

```tsx
import { Toaster } from "@/components/ui/sonner";
import { toast } from "sonner";

{
  /* 실습 코드 */
}

{
  /* 1. 앱 루트에 Toaster 배치 — 전체 앱에서 toast() 호출 가능 */
}
<Toaster />;

{
  /* 2. 버튼 클릭 시 toast 호출 */
}
<Button
  onClick={() => {
    toast("토스트 메시지", {
      position: "top-center",
    });
  }}
>
  버튼!
</Button>;
```

> `<Toaster />` 토스트가 렌더링될 **위치(포탈)**를 지정한다. 앱 전체에서 `toast()`를 호출할 수 있도록 루트에 한번만 배치한다.

- `toast()` 종류

```tsx
import { toast } from "sonner";

// 기본 메시지
toast("파일이 저장되었습니다.");

// 성공
toast.success("저장 완료!");

// 에러
toast.error("저장에 실패했습니다.");

// 경고
toast.warning("저장 공간이 부족합니다.");

// 정보
toast.info("새로운 업데이트가 있습니다.");

// 로딩 → 완료로 전환
const id = toast.loading("저장 중...");
toast.success("저장 완료!", { id }); // 같은 id로 교체

// 액션 버튼 포함
toast("삭제하시겠습니까?", {
  action: {
    label: "실행 취소",
    onClick: () => console.log("취소됨"),
  },
});
```

#### 📍 position 옵션

| 값              | 위치                      |
| :-------------- | :------------------------ |
| `top-left`      | 왼쪽 위                   |
| `top-center`    | 가운데 위 ← 실습에서 사용 |
| `top-right`     | 오른쪽 위 (기본값)        |
| `bottom-left`   | 왼쪽 아래                 |
| `bottom-center` | 가운데 아래               |
| `bottom-right`  | 오른쪽 아래               |

- `<Toaster /> 전역 설정

```tsx
<Toaster
  position="top-center" // 기본 위치
  duration={3000} // 자동 닫힘 시간 (ms), 기본 4000
  richColors // 색상 강조 (success=초록, error=빨강)
  closeButton // 닫기 버튼 표시
/>
```

- Sonner가 내장 Toast보다 좋은 이유
- shadcn/ui에 자체 `Toast` 컴포넌트도 있지만 공식 문서에서 Sonner를 권장한다.

#### 🍞 내장 Toast vs Sonner 비교

| 항목            | 내장 Toast                      | Sonner                 |
| :-------------- | :------------------------------ | :--------------------- |
| **사용 복잡도** | `useToast()` 훅 + Provider 필요 | `toast()` 함수 한 줄   |
| **스택 처리**   | 수동 관리                       | 자동으로 쌓임          |
| **애니메이션**  | 기본                            | 세련된 스택 애니메이션 |
| **설정**        | 복잡                            | 간단                   |
