## 2.4 Shadcn/ui

### Shadcn/ui

- shadcn/ui는 Radix UI 기반의 접근성 좋은 컴포넌트에 Tailwind CSS로 스타일을 입힌, "복사해서 쓰는" UI 컴포넌트 모음이다.
- 일반적인 npm 패키지 설치 방식이 아닌, 컴포넌트 소스 코드를 프로젝트 안으로 직접 복사해서 사용하는 것이 가장 큰 특징이다.

> 공식 문서: https://ui.shadcn.com
> 설치 없이 코드만 복사해서 사용하는 "Not a library" 철학의 UI 도구이다.

- 핵심 특징 4가지
  | 특징 | 설명 |
  | :--- | :--- |
  | 📦 **설치가 아닌 복사** | `npm install`이 아니라 컴포넌트 코드를 내 프로젝트 안에 직접 가져온다 |
  | ✏️ **100% 커스터마이징** | 소스 코드가 내 프로젝트 안에 있으므로 자유롭게 수정 가능하다 |
  | ♿ **접근성 내장** | Radix UI 기반으로 키보드 탐색, 스크린 리더 등 WAI-ARIA를 준수하다 |
  | 🎨 **Tailwind 스타일링** | Tailwind CSS 클래스로 스타일이 구성되어 있어 기존 스타일과 자연스럽게 통합된다 |

---

### 기존 UI 라이브러리와 차이점

- Material UI, Ant Design 같은 기존 라이브러리는 npm으로 설치, 컴포넌트를 import해 쓴다.
- shadcn/ui는 컴포넌트 코드 자체가 프로젝트의 일부가 된다.

- 기존 방식

```bash
# 패키지를 설치
npm install @mui/material
```

```tsx
// import해서 사용
import { Button } from "@mui/material";

// 내부 수정 불가 → 덮어쓰기만 가능
// 스타일 오버라이드가 복잡함
```

- shadcn/ui 방식

```bash
# CLI로 컴포넌트 추가
npx shadcn@latest add button

# → components/ui/button.tsx 생성됨
# → 직접 수정 가능!
```

```tsx
import { Button } from "@/components/ui/button";
```

- Tailwind 방식:

```jsx
<div className="p-4 bg-white rounded-lg shadow-md">카드</div>
```

- 📊 비교표

| 항목                    | 기존 UI 라이브러리              | shadcn/ui                               |
| :---------------------- | :------------------------------ | :-------------------------------------- |
| **설치 방식**           | `npm install`                   | CLI로 코드 복사                         |
| **소스 코드 위치**      | `node_modules` (수정 불가)      | 내 프로젝트 안 (자유롭게 수정)          |
| **스타일 커스터마이징** | props 또는 오버라이드 CSS       | Tailwind 클래스 직접 수정               |
| **번들 크기**           | 사용 안 해도 포함됨             | 추가한 컴포넌트만 포함                  |
| **디자인 일관성**       | 라이브러리 디자인 시스템에 종속 | 내 프로젝트 디자인 시스템으로 완전 통합 |

---

### 3. 기술 스택 구성

- shadcn/ui는 여러 라이브러리를 조합해 만들어진 레이어이다. 각 레이어가 어떤 역할을 하는 지 이해하면 훨씬 쉬워진다.
  ┌──────────────────────────────────────┐
  │ shadcn/ui │ ← 스타일 + 컴포넌트 구조 정의
  ├──────────────────────────────────────┤
  │ Tailwind CSS │ ← 스타일링 (유틸리티 클래스)
  ├──────────────────────────────────────┤
  │ Radix UI │ ← 접근성 + 비제어 컴포넌트 로직
  ├──────────────────────────────────────┤
  │ React │ ← UI 렌더링
  └──────────────────────────────────────┘

- 🥞 각 레이어의 역할

| 레이어                       | 역할                                              | 예시                                        |
| :--------------------------- | :------------------------------------------------ | :------------------------------------------ |
| **Radix UI**                 | 접근성을 갖춘 헤드리스(스타일 없는) 컴포넌트 제공 | Dialog 열기/닫기 로직, Dropdown 키보드 탐색 |
| **Tailwind CSS**             | 유틸리티 클래스로 스타일 적용                     | `rounded-md bg-primary text-white`          |
| **class-variance-authority** | variant(변형)별 클래스를 타입 안전하게 관리       | 버튼의 default / outline / ghost 변형       |
| **clsx / cn()**              | 조건부 클래스 병합 유틸 함수                      | `cn("base", isActive && "active")`          |

---

### 4. 설치 및 초기 설정

#### 1. Vite + React 프로젝트 기준

```bash
# 1. 새 Vite + React 프로젝트 생성
npm create vite@latest my-app -- --template react-ts
cd my-app && npm install

# 2. Tailwind CSS 설치
npm install tailwindcss @tailwindcss/vite

# 3. shadcn/ui 초기화
npx shadcn@latest init

# 4. 컴포넌트 추가 (예: Button)
npx shadcn@latest add button
npx shadcn@latest add dialog card input
```

> `npx shadcn@latest add [컴포넌트명]` 실행 시 `src/components/ui/` 폴더에 컴포넌트 소스 파일이 생성된다.

#### 2. 자동 생성되는 파일 구조

src/
├── components/
│ └── ui/
│ ├── button.tsx ← 직접 수정 가능!
│ ├── dialog.tsx
│ ├── input.tsx
│ └── card.tsx
├── lib/
│ └── utils.ts ← cn() 유틸 함수
└── index.css ← CSS 변수 (테마 토큰)

---

### 05. 컴포넌트 예시

- Button

* 버튼은 `variant`와 `size` prop스타일을 변형한다.

```tsx
import { Button } from "@/components/ui/button";

function MyPage() {
  return (
    <div className="flex gap-2">
      {/* variant로 스타일 변형 */}
      <Button>Default</Button>
      <Button variant="outline">Outline</Button>
      <Button variant="ghost">Ghost</Button>
      <Button variant="destructive">삭제</Button>
      <Button variant="secondary">Secondary</Button>

      {/* size 변형 */}
      <Button size="sm">작게</Button>
      <Button size="lg">크게</Button>

      {/* 비활성화 */}
      <Button disabled>비활성</Button>
    </div>
  );
}
```

---

### 06. cn() - 클래스 병합 유틸 함수

- shadcn/ui를 설치하면 `src/lib/utils.ts`에 `cn()`함수가 자동 생성된다.
- 조건부 클래스 병합을 타입 안전하게 처리해주는 유틸이다.

```ts
// src/lib/utils.ts
import { clsx, type ClassValue } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

```tsx
import "./App.css";
import { Button } from "./components/ui/button";
import { cn } from "./lib/utils";

function App() {
  const isActive = true;

  return (
    <div>
      <Button>버튼!</Button>
      <div
        className={cn(
          "w-10 text-sm",
          isActive ? "text-green-500" : "text-red-500",
        )}
      >
        isActive
      </div>
        );
}

export default App;
```
